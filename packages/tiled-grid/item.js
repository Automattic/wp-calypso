/** @format */

export class Jetpack_Tiled_Gallery_Item {
	// eslint-disable-next-line no-unused-vars
	constructor( attachment_image, needs_attachment_link, grayscale ) {
		this.image = attachment_image;
	}
}
