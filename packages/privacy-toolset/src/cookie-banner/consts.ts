import { Buckets } from './types';

export const allBucketsTrue: Buckets = {
	essential: true,
	analytics: true,
	advertising: true,
};

export const defaultBuckets: Buckets = {
	essential: true,
	analytics: true,
	advertising: false,
};
