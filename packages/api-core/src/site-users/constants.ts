/**
 * The `users/me` meta key the sitewide plan-expiry notice stamps when it is
 * dismissed. Written by the server as a Unix timestamp in seconds, regardless
 * of what the client sends.
 */
export const PLAN_EXPIRY_NOTICE_DISMISS_META_KEY = 'wpcom_plan_expiry_notice_dismiss_wp_admin';
