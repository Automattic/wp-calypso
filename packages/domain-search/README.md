# Domain Search

`@automattic/domain-search` contains shared components for the domain search experiences across Automattic.

## Installation

```sh
yarn add @automattic/domain-search
```

## Usage

Import the components that you need.

`import { DomainSearchInput } from '@automattic/domain-search'`.
