// Default to "fallback" build polyfills.
// In the "evergreen" Calypso build, we rewrite the import, so that the
// evergreen polyfills are used instead.

/**
 * Internal dependencies
 */
import './fallback';
