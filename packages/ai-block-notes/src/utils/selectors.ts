export const BLOCK_NOTE_CONTENT_SELECTOR = [
	'.editor-collab-sidebar-panel__note-content',
	'.editor-collab-sidebar-panel__user-comment',
].join( ', ' );

export const BLOCK_NOTE_THREAD_SELECTOR = [
	'[id^="note-thread-"]',
	'[id^="comment-thread-"]',
].join( ', ' );
