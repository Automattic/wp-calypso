import { before } from 'lodash';
/**
 * Internal dependencies
 */
import handler from '../src/handler';

describe( 'JS error reporting', () => {
	it( 'logs the error', async () => {} );

	it( 'throttles if subsequent request sent within the throttling interval', () => {} );
} );
