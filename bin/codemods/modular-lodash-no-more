#!/usr/bin/env node

/*
	This codemod converts modular lodash imports to ES2015-style imports

	How to use:
	./bin/codemods/modular-lodash-no-more path-to-transform/
*/

/**
 * Internal dependencies
 */
const { runCodemod } = require( './src/helpers' );

runCodemod( ( config, args ) => [
	// jscodeshift options
	'--transform=bin/codemods/src/modular-lodash-no-more.js',
	...config.jscodeshiftArgs,

	// Transform target
	...args,
] );
