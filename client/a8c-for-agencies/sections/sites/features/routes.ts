import { JetpackFeatureRoutes } from './jetpack/jetpack-routes';

export function FeatureRoutes( basePath: string ) {
	// Load feature routes
	JetpackFeatureRoutes( basePath );

	// Todo: Example of other feature routes
	//OverviewFeatureRoutes();
	//SecurityFeatureRoutes();
}
