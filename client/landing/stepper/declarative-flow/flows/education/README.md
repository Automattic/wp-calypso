# Education Flow

The Education flow lives at `/setup/education`.

It validates an Education Program invite code after account creation/sign-in, then sends the user through domain selection and checkout with the Student plan preselected.
