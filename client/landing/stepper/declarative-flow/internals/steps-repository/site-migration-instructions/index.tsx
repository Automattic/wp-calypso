import React from 'react';
import type { Step } from '../../types';

const SiteMigrationInstructions: Step = function () {
	return <div>New migration instructions screen</div>;
};

export default SiteMigrationInstructions;
