# Connected Application Item JSX

`connected-application-item` renders a single connected application. It's meant to be used with the `connected-applications` component at `me/connected-applications`.
