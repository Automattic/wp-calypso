/**
 * Module variables
 *
 * @format
 */

const sections = [
	{
		name: 'reader',
		paths: [ '/', '/read' ],
		module: 'reader',
		secondary: true,
		group: 'reader',
	},
];

module.exports = sections;
