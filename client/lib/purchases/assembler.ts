import { Purchases } from '@automattic/data-stores';

export const createPurchaseObject = Purchases.utils.createPurchaseObject;
export const createPurchasesArray = Purchases.utils.createPurchasesArray;
