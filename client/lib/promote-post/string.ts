import { useTranslate } from 'i18n-calypso';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const BlazePressStrings = () => {
	const translate = useTranslate();
	translate( 'Promote your posts' );
	translate( 'An easy way to reach a larger WordPress.com community of blogs and sites.' );
	translate( 'It takes less than 5 minutes.' );
};
