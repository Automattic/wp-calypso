/** @format */
const exported = {
	RECORDS_LIST_KEY: 'records-list',
	SYNC_RECORD_NAMESPACE: 'sync-record-',
	LIFETIME: '2 days',
};

export default exported;
const { RECORDS_LIST_KEY, SYNC_RECORD_NAMESPACE, LIFETIME } = exported;
export { RECORDS_LIST_KEY, SYNC_RECORD_NAMESPACE, LIFETIME };
