import { __ } from '@wordpress/i18n';

export function getAppSetupTitle(): string {
	return __( 'Set up using an app' );
}

export function getSMSSetupTitle(): string {
	return __( 'Set up using SMS' );
}
