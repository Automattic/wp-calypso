import { Card, CardBody, __experimentalVStack as VStack } from '@wordpress/components';

export default function CreateSiteRedirect() {
	return (
		<Card>
			<CardBody>
				<VStack spacing={ 4 }>
					<h1>Create Site Redirect</h1>
				</VStack>
			</CardBody>
		</Card>
	);
}
