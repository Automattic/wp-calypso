/**
 * External dependencies
 */
import { expect } from 'chai';

/**
 * Internal Dependencies
 */
import { }

describe( 'actions', () => {
	it( 'should work', () => {
		expect( true ).to.be.false;
	} );
} );
