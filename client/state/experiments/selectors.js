/**
 * Get variation for a specific experiment
 *
 * @param experiment string The experiment to get the variation for
 */
export const getVariationForUser = experiment => {
	return experiment;
};

export const isUserAnon = () => true;
