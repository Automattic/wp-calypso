/** @format */

/**
 * Internal dependencies
 */
import importerLocks from './importer-locks/reducer';
import { combineReducers } from 'state/utils';

export default combineReducers( {
	importerLocks,
} );
